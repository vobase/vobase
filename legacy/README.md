# legacy/

Frozen, archived templates kept for reference only. Not part of the workspace; not in CI.

- `template-v1/` — pre-canonical-shape template. Pinned to `@vobase/core@0.33.0` (the last release that exported its declarative API). Use the v3+ `@vobase/template` for new projects.
