---
title: "{Project Name}"
title_color: mauve
eyebrow: "DEMO · {CLIENT GROUP}"
contact: "yash@voltade.com"
lede: "**A Voltade demo for {Client}** — {one-line description of what it does and a verb-driven adjective like 'gets smarter every day'}."
quick_access:
  - "**Web:** [{slug}.voltade.app](https://{slug}.voltade.app)"
  - "**WhatsApp:** +65 {phone} ([wa.me/65{phone}](https://wa.me/65{phone}))"
---

ACCESS
Open [`{slug}.voltade.app`](https://{slug}.voltade.app) and sign in with an **email OTP** — a one-time code is sent to your inbox. Allowlisted domains: `@{client}.sg` · `@{client}.com.sg` · `@voltade.com`.

First sign-in auto-enrols you as a member of the **{Client} Group** workspace.

TRY THE AI ON WHATSAPP

1. **First time?** [Tap here to register your number →](https://wa.me/65{phone}?text=%2Flink%20{token}) WhatsApp opens with the command `/link {token}` pre-filled. Just hit send.
2. **Or scan the QR code** from the web app: sign in → **Channels** → row for *{Project Name} (WhatsApp)* → click the **Link QR** button. Point your phone camera at the QR; it opens WhatsApp with the same command pre-filled.
3. Once linked, just chat normally — *"{Sample Q one?}"* · *"{Sample Q two?}"* · *"{Sample Q three?}"*
4. **{Project Name}** replies within seconds. The same conversation appears in the web **Inbox**.
5. Staff can take over, drop a private note for a teammate, or let the AI keep going.

---

## What the system does

A SELF-IMPROVING AI ASSISTANT
{Project Name} doesn't just answer — it **learns**. Every time a staff member corrects it, leaves a note, or overrides a reply, the AI updates its own memory. The next conversation is a little better. Over weeks, it picks up the company voice, the {domain} playbook, who handles what, and the small exceptions that make {Client} *{Client}*.

There are three layers of memory: what it knows about itself, what it knows about each {customer/candidate/employee}, and what it has learned from each staff member it works with.

ADAPTIVE SOFTWARE, NOT STATIC FORMS
Most {domain} tools are forms. This one shapes itself around how {Client} actually works. Staff teach it by example — a private note, a correction, a one-line rule — and the workflow adjusts. New policy on {something}? Tell the AI once. New escalation rule for {something else}? It remembers from then on.

ONE INBOX FOR WHATSAPP AND WEB
Every conversation — {persona} on WhatsApp, {persona} on the portal, walk-in via email — lands in a single inbox. Staff reply once, in plain language, and the message goes back through whichever channel the person used. No tab juggling, no "which system was that in".

BUILT-IN {DOMAIN} FEATURES
- **{Feature one}** — short description of what it covers, ending with a verb-led example (e.g., "add candidates, schedule interviews, move stages with one click").
- **{Feature two}** — short description.
- **{Feature three}** — short description.
- **Internal notes** — `@`-mention a teammate inside any conversation. They get pinged on WhatsApp once they verify their number. Notes are private to staff; the {customer} never sees them.
- **Policy knowledge** — the AI reads from a shared knowledge base ({list of files}). Update the file, the AI updates its answers.

GUARDRAILS
The AI knows what it shouldn't do on its own. {Risky topic one} always escalates to a human. {Risky topic two} routes to {team}. {Something it never does}. These rules live in plain text and can be edited without touching code.

HUMAN-IN-THE-LOOP CHANGES
When the AI wants to update a {thing}, schedule a {thing}, or change a record, it can either do it directly (for low-stakes actions) or **propose** the change for staff approval. Every proposal shows up in one place with a clean accept/reject view. Nothing happens behind your back.

AUTOMATIONS THAT RUN THEMSELVES
The AI checks in on its own schedule — {a daily briefing at time}, {a sweep through stale conversations every N minutes}, {a nudge for {thing} that didn't get {action}}. Staff don't have to remember; the AI does.
