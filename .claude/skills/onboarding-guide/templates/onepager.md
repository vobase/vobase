---
title: "{Client} × Voltade — Demo *Sandbox*"
title_color: purple
eyebrow: "DEMO ONBOARDING"
contact: "yash@voltade.com"
lede: "A live preview of **{AgentName}**, an AI {role} built for {client}'s {scope} ({list, of, flows}), on Voltade's agent-native helpdesk platform."
quick_access:
---

1. OPEN + SIGN IN
Go to `{client}-demo.voltade.app`, enter your `@{client}.com` email, paste the 6-digit OTP from your inbox. Only `@{client}.com` and `@voltade.com` addresses allowed.

2. ADD YOURSELF AS A TESTER — TWO WAYS
**(a) Web chat — fastest, no setup.** Open `{client}-demo.voltade.app/test-web` and start typing. The widget posts straight to the same agent your inbox sees.

**(b) WhatsApp — the real production channel.**

1. From the dashboard go to **Channels** → click **Connect platform sandbox** to claim a shared Voltade WhatsApp number from our test pool.
2. A panel appears with a QR + a one-line command like `/link a8f4b2c9e0d1`. Scan the QR with your phone's WhatsApp — it opens the sandbox chat with the link command pre-filled. Send it.
3. Your phone is now a tester. Send any normal message ("hi, what's my {something} this month?") and watch it land in **Inbox** as a real conversation. {AgentName} replies back on WhatsApp.

3. SUGGESTED PROMPTS
> "Replace with a concrete, domain-specific question a real user would ask."
> "Include a small wrinkle (a price, a date, an exception) so the answer needs real context."
> "Add one escalation-worthy edge case to show guardrails."

4. WHAT'S PRE-LOADED
{N} seeded conversations ({short, list}) · {AgentName} knowledge base ({BUSINESS.md, pricing, policy.md, ...}) · {N} staff teammates with escalation rules ({Name / Name / Name}) · Live activity feed of every wake, tool call, and internal note.

5. TWO THINGS WORTH NOTICING
:::cards
### ADAPTIVE SOFTWARE
Same codebase as every Voltade tenant — agent voice, knowledge base, pricing, escalation rules, and staff roster are all **config + seed**, not code. We reshaped this demo into {Client} in hours. Your real deployment edits the same files.
### SELF-LEARNING
{AgentName} writes lessons to its own memory after every conversation. Staff coach it via `@`-mention internal notes — next reply applies the rule. Per-customer + per-staff memory persists across threads. Sharper with use, no retraining.
:::
