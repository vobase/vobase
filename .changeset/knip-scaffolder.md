---
"create-vobase": patch
---

Generate knip.json for scaffolded projects

Projects created with `bun create vobase` now include a pre-configured `knip.json` for detecting unused files, dependencies, and exports. Run `bun run knip` to analyze your project.
