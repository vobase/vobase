---
"@vobase/template": patch
---

Use per-module Hono typed client for system routes, replacing manual response interfaces and unsafe casts. Add missing shadcn UI components (calendar, checkbox, textarea, kbd) and fix pre-existing data-grid type errors. Update AGENTS.md with upstream template upgrade instructions.
