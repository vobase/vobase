/**
 * Agent-facing surfaces for the team module. Materializers only — staff ids are
 * wake-time data, so the factory pattern is used.
 */

export { buildTeamMaterializers as buildMaterializers } from './materializers'
