/**
 * channel-whatsapp module schema — no dedicated tables in Phase 1.
 * WhatsApp channel state lives in inbox.channel_instances (type='whatsapp').
 * Placeholder to satisfy module-shape contract.
 */
