/** Test convenience re-export of the harness mock-stream helper. */

export type { MockStreamEvent, MockStreamRun, StreamFn } from '@server/harness'
export { mockStream, mockStreamTurns } from '@server/harness'
