import { describe, expect, it } from 'bun:test'

// Placeholder — Parcel R will expand with useKeyboardNav mount + j/k/Enter/Esc/Cmd+Enter assertions.
// See src/hooks/use-keyboard-nav.ts and src/hooks/__tests__/use-keyboard-nav.test.tsx (Parcel R owns those).

describe('keyboard nav (placeholder)', () => {
  it('placeholder passes until Parcel R lands', () => {
    expect(true).toBe(true)
  })
})
